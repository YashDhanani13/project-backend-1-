
// ---------------------------------------------------------------------
//recevice  message : -

// socket.on('send message', (data) => {
//     const { roomId, text } = data
// })

// db save

//send back to user - 2  

// io.to(roomId).emit('recevice  message ', {
//     text,
// })

// --------------------------------------------------------------
// delete the message : -  
  
// io.on(delete message, ({ messageId, roomId }) => {})


//db save :-    
 
// send back to user-2

// io.to(roomId).emit('message_deleted', {
//     messageId,
// })
